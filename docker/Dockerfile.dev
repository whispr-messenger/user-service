FROM node:20-alpine

# Install bash and wget for packages that need it (like just-install)
RUN apk add --no-cache bash wget

WORKDIR /app

COPY package*.json ./

RUN npm ci --ignore-scripts && npm cache clean --force

EXPOSE 3000 9229

CMD ["npm", "run", "start:dev"]